#include <stdio.h>

int main()
{
int a, b;

printf("Enter number a\n");
scanf("%d", &a);
printf("Enter number b\n");
scanf("%d", &b);

printf("The sum is %d\n", a+b);

return 0;
}
// ctrl + shift + L  for selecting same word, number or any thing
// ctrl + shift + P for command palate
// ctrl + p for search
// ctrl + l for selecting a line
// ctrl + b for toggling side bar
// ctrl + g for goto line
// ctrl + / for comment out a line 
// ctrl + \ for splitting
// alt + up/down for moving a line upward or downward
// alt + shift + up/down for duplicating a line
// ctrl + alt for editing multiple line